<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:seo_title) ? yield(:seo_title) + " | Chris's Portfolio" : "Chris's Portfolio" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'Chris\'s portfolio is a portfolio of all the work that I, Christopher Anderson, have done' %>" >
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

</head>
  <body>
    <div id="background-img" data-imgpath="<%= asset_path('parkwaydrive.jpg') %>"></div>
    <%= render 'layouts/navigation' %>

    <div class='container'>
      <%= render 'layouts/messages' %>
      <%= yield %>
    </div>
  </body>
</html>
